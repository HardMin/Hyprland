
# Set programs that you use
$terminal = kitty
$fileManager = nemo
$menu = wofi --show drun
# $navigator = google-chrome-stable
$navigator = brave
# $browser = flatpak run app.zen_browser.zen 
$browser = $navigator
